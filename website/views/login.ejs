<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <style>
        body { display: flex; align-items: center; justify-content: center; min-height: 100vh; }
        .auth-card { width: 100%; max-width: 400px; padding: 2.5rem; border-radius: 1.5rem; background: #fff; box-shadow: 0 10px 25px rgba(0,0,0,0.05); border: 1px solid #f3f4f6; }
    </style>
</head>
<body class="bg-light">
    <div class="auth-card fade-in">
        <div class="text-center mb-5">
            <h3 class="fw-bold">Welcome Back</h3>
            <p class="text-secondary">Please sign in to continue</p>
        </div>

        <% if(error_msg.length > 0) { %>
            <div class="alert alert-danger py-2 small rounded-3 border-0 bg-danger bg-opacity-10 text-danger"><%= error_msg %></div>
        <% } %>
        <% if(success_msg.length > 0) { %>
            <div class="alert alert-success py-2 small rounded-3 border-0 bg-success bg-opacity-10 text-success"><%= success_msg %></div>
        <% } %>
        <% if(error.length > 0) { %>
            <div class="alert alert-danger py-2 small rounded-3 border-0 bg-danger bg-opacity-10 text-danger"><%= error %></div>
        <% } %>

        <form action="/login" method="POST">
            <div class="mb-3">
                <label for="username" class="form-label fw-bold small text-uppercase text-secondary">Username</label>
                <input type="text" id="username" name="username" class="form-control bg-light border-0 py-2" required>
            </div>
            <div class="mb-4">
                <div class="d-flex justify-content-between">
                    <label for="password" class="form-label fw-bold small text-uppercase text-secondary">Password</label>
                </div>
                <input type="password" id="password" name="password" class="form-control bg-light border-0 py-2" required>
            </div>
            <button type="submit" class="btn btn-primary w-100 py-2 rounded-pill fw-bold shadow-sm">Sign In</button>
        </form>

        <div class="text-center mt-4 pt-3 border-top">
            <small class="text-muted">Registering as new Admin? <a href="/register" class="text-decoration-none fw-bold">Sign Up</a></small>
            <br>
            <small><a href="/" class="text-decoration-none text-secondary">Back to Home</a></small>
        </div>
    </div>
</body>
</html>
